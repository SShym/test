export interface Hero {
  id: number;
  name: string;
}

export interface HeroDetails {
  id: number;
  name: string;
  films: number[];
  starships: number[];
}

export interface HeroDetails {
  id: number;
  name: string;
  films: number[];
  starships: number[];
}